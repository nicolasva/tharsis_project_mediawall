<h1>Nouveau zip</h1>

<% form_for(@zip, :html => { :multipart => true }) do |f| %>
	<%= render(:partial => 'form', :locals => { :f => f }) %>	
  <p>
    <%= f.submit 'Creer' %>
  </p>
<% end %>

<%= link_to 'Back', zips_path %>
